apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: bucket
    user: user
    password: password
    url: http://influxdb:8086
    isDefault: true
    editable: true
